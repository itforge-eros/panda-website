html
	head
		meta(charset="utf-8")
		meta(name="viewport", content="initial-scale=1, width=device-width")
		title Page Title Here
		include parts/stylesheet
		include parts/scripts
	body.page-file-name
		include parts/nav
		div#app
			div.content
				div.container
					div.page-title-wrap
						h1.page-title จัดการสิทธิ์
					div
						a.button.marg-b-1(href="#")
							i.fas.fa-plus-circle
							| สร้างสิทธิ์ใหม่
						form(action="")
							label(for="role-name") พิมพ์ชื่อสิทธิ์และกด Enter
							input(type="text", name="role-name")
					table.styled
						each item in [1,2,3,4,5,6,7,8]
							tr
								td Student
								td.actions
									a(href="/manage-role/" + item + "/users")
										i.fas.fa-user
									a(href="/manage-role/" + item)
										i.fas.fa-cog
									a(href="#")
										i.fas.fa-trash
