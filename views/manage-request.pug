html
	head
		include parts/meta
		title จัดการคำร้อง - KMITL OSRS
		include parts/stylesheet
		include parts/scripts
	body.manage-request
		include parts/nav
		div#app
			div.content
				div.container
					div.page-title-wrap
						h1.page-title จัดการคำร้อง
					ul.tabs
						li.selected
							a(href="#") รอการอนุมัติ
						li
							a(href="#") อนุมัติแล้ว
					div
						if !canSee
							h1 no request
						each m in canSee
							a.card.shadow.hoverable.request-card(href=m.name)
								div.req-info
									h1.card-title= m.fullName
									p.grayed= m.status

//- สอนผมด้วยนะครั บ VUE อยากลองทำเองก่อนตรง ที่สามารถสลับหน้าไปอนุมัติ ! ได้ !! 