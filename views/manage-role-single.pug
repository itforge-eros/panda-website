html
	head
		include parts/meta
		title จัดสิทธิ์ Student - KMITL OSRS
		include parts/stylesheet
		include parts/scripts
	body.manage-role-single
		include parts/nav
		div#app
			div.content
				div.container
					if status == "success"
						div.notice.success
							i.fas.fa-check-circle
							|  บันทึกข้อมูลเรียบร้อย
					else if status != ""
						div.notice.warning
							i.fas.fa-exclamation-circle
							|  ยังไม่ได้บันทึกข้อมูล กรุณาตรวจสอบข้อมูลอีกครั้ง (#{status})
					div.page-title-wrap
						h2.page-pretitle จัดการสิทธิ์
						h1.page-title {{ name || "สิทธิ์ใหม่" }}
					form(action="save", method="POST")
						div.marg-b-1
							label(for="role-name") ชื่อสิทธิ์
							input(type="text", name="name", v-model="name")
						div.marg-b-1
							label อำนาจ
							table
								each item in [1,2,3,4,5,6,7,8]
									tr
										td
											label.control.checkbox
												input(type="checkbox", name="permissions", value="REQUEST_CREATE_ACCESS")
												span.control-indicator
												| REQUEST_CREATE_ACCESS
						button.raised
							i.fas.fa-save
							| บันทึก
		script(src="/public/scripts/manage-role-single.js")
		script
			| app.name = "#{orgData.name}"
