html
	head
		include parts/meta
		title Manage Request
		include parts/stylesheet
		include parts/scripts
	body.manage-request-single
		include parts/nav
		div#app
			div.content
				div.container
					div.page-title-wrap
						h1.page-title รายละเอียดคำร้อง
						h2.page-subtitle คำร้อง ##{id}
					table.styled.striped.marg-b-2
						tr.status.pending
							td สถานะ
							td รอการอนุมัติ (1 จาก 2)
						each item in reqInfo
							tr
								td= item.name
								td= item.data
					a.button.raised(onclick="showModal('modal')")
						i.fas.fa-check
						| อนุมัติ
					a.button.raised.color-red(onclick="showModal('modal')")
						i.fas.fa-times
						| ปฏิเสธ
			div.modal#modal
				div.card
					span.close(onclick="closeModal('modal')")
						i.fas.fa-times-circle
					h1.modal-title กรุณากรอกรหัสผ่าน
					div.input-wrap
						input.fluid(name="report", type="password")
					button.raised ยืนยัน
		script(src="/public/scripts/manage-request-single.js")
		script
			| function showModal(modal) {
			| 	document.getElementById(modal).classList.add("show")
			| }
			| function closeModal(modal) {
			| 	document.getElementById(modal).classList.remove("show")
			| }
			| if (navigator.userAgent.indexOf('Safari') != -1 && navigator.userAgent.indexOf('Chrome') == -1) {
			| 	var modal = document.getElementsByClassName("modal");
			| 	for (i = 0; i < modal.length; i++) modal[i].classList.add("ios");
			| }
